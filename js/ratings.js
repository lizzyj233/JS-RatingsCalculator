function collectratings(params) {
    let ratings = {
        count : 0,
        sum : 0,
        average : 0
    };
    let rating = 0;
};

let elements = document.querySelectorAll("rating");

elements.forEach(function(element){

});